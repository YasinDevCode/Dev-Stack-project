1.Ans: JSX is a syntax that lets you write HTML-like code inside JavaScript. It makes UI code easier to read and build.

2. Ans: Props pass data from parent to child and are read-only. State is internal data of a component that changes and triggers re-render.
3.Ans: useState creates and updates state in a component. I used it to manage lists and update the UI.
4.Ans:useState creates and updates state in a component. I used it to manage lists and update the UI.
5. Ans:Each item needs a unique key so React can track changes and re-render efficiently.
6.Ans: Conditional rendering means showing UI based on conditions. I used it to display “Empty Stack” when the stack was empty.
7. Ans: Data is passed from parent to child via props. A child sends data back to the parent using a callback function.